services:
  app:
    image: todo-front-dev
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ./:/usr/src/app
    ports:
      - 5172:5173
    environment:
      - VITE_BACKEND_URL=http://localhost:3000
    container_name: todo-fronted-dev